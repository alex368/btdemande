<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Maintenance | Fidelissimo</title>
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon/favicon.ico') }}">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="min-vh-100 d-flex align-items-center" style="background: linear-gradient(135deg, #f7f2e8 0%, #eef2ff 50%, #dbeafe 100%);">
    <main class="container py-5">
        <section class="mx-auto bg-white shadow-lg overflow-hidden" style="max-width: 900px; border-radius: 24px;">
            <div class="row g-0">
                <div class="col-lg-5 p-5 text-white" style="background: linear-gradient(160deg, #163a70 0%, #214995 50%, #0f766e 100%);">
                    <img src="{{ asset('image/logo.png') }}" alt="Logo Fidelissimo" width="88" class="mb-4">
                    <span class="badge bg-light text-dark rounded-pill px-3 py-2 mb-3">Mode maintenance</span>
                    <h1 class="display-6 fw-bold mb-3">On améliore la plateforme</h1>
                    <p class="mb-0 opacity-75">
                        Une courte intervention est en cours pour stabiliser et mettre à jour l'espace Fidelissimo.
                    </p>
                </div>
                <div class="col-lg-7 p-5">
                    <h2 class="fw-bold mb-3" style="color:#163a70;">Retour prévu très bientôt</h2>
                    <p class="text-secondary mb-4">
                        Le service revient dès la fin de l'intervention. Les administrateurs peuvent toujours accéder à la connexion si nécessaire.
                    </p>
                    <div class="d-flex flex-wrap gap-3">
                        <a href="{{ path('app_login') }}" class="btn rounded-0 px-4 py-2" style="background:#FBBB3B; color:#163a70;">Espace administrateur</a>
                    </div>
                </div>
            </div>
        </section>
    </main>
</body>
</html>
