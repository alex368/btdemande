{% extends 'base.html.twig' %}

{% block title %}Ajouter une prestation{% endblock %}

{% block body %}
  <main class="col-md-12 col-lg-12 p-2">
<div class="container mt-5">
      <h1 class="form-label text-center bebas text-color-blue mb-5 mt-5">Ajouter une prestation</h1>

    {{ form_start(form) }}
     {{ form_row(form.title) }}
        {{ form_row(form.description) }}
        {{ form_row(form.price) }}
        <button class="btn btn-primary mt-5">Enregistrer la prestation</button>
    {{ form_end(form) }}
</div>
</main>
{% endblock %}
