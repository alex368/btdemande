{% extends 'base.html.twig' %}

{% block title %}Détail prestation{% endblock %}

{% block body %}
<div class="container mt-4">


    <h1 class="mb-4 text-color-blue bebas">Détail de la prestation</h1>

    <div class="card">
        <div class="card-body">

            <table class="table table-bordered">
                <tr>
                    <th>Titre</th>
                    <td>{{ services.title }}</td>
                </tr>

                <tr>
                    <th>Description</th>
                    <td>{{ services.description }}</td>
                </tr>

                <tr>
                    <th>Prix</th>
                    <td>{{ services.price }}</td>
                </tr>

               
            </table>

            <div class="d-flex gap-2 mt-3">
                <a href="{{ path('app_service_product') }}" class="btn btn-secondary">Retour</a>
                <a href="{{ path('app_service_product_edit', { id: services.id }) }}" class="btn btn-warning">Modifier</a>

                <form method="post"
                      action="{{ path('app_service_product_delete', { id: services.id }) }}"
                      onsubmit="return confirm('Confirmer la suppression de cette prestation ?');">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete-serviceProduct-' ~ services.id) }}">
                    <button class="btn btn-danger">Supprimer</button>
                </form>
            </div>

        </div>
    </div>

</div>
{% endblock %}
